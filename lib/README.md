# Library (`/lib`)

This directory contains shared utilities, constants, environment helpers, and state management utilities.

## Modules
- `utils.ts`: General formatting, styling helpers (e.g. Tailwind class merges).
- `constants.ts`: System constants, safety thresholds, and route status identifiers.
- Journey state coordination utilities.
